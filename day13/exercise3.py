# Reverse String
# Write a program that takes a string as input and reverses it, then prints the reversed string.

# Hints:
# Define a function called reverse_string(input_string) that takes a string as input and returns the reversed string.
# Use string slicing to reverse the string.
print("Bienvenido para invertir palabra!")
input_string = input("Ingrese palabra:\n")
new_string = input_string[::-1]
print(new_string)