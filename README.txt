#Pre Requisitos
# a. que tu rama previamente este pusheada
# b. asegurarte que no lleves cambios a la rama main
# c. estar en el proyecto correcto

# 1. asegurarte que estes en la rama main
# 2. si estas en la rama main traer posibles cambios 
    # git pull orgin main
# 3. cambias a tu rama
    # git checkout raquel_branch
# 4. fusionas los cambios de la rama main con tu rama
    # git merge main
# 5. para subir cambios haces los siguientes comandos
    # git add . (añades cambios)
    # git commit -m "mensaje significativo" (guardas cambios)
    # git push (subes los cambios)